import styled from "@emotion/styled";

export const StyledVersion = styled.span`
  position: fixed;
  top: 0.5rem;
  right: 0.5rem;
  font-size: small;
  font-style: italic;
  margin-right: 0.5rem;
`;
